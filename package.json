{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "turbo test",
    "build": "turbo build",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "turbo": "^1.10.6"
  },
  "overrides": {
    "file-system-cache": "2.3.0"
  }
}